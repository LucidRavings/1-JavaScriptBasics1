/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

//CODE HERE
let gitDefinition = "Git is a version control software that helps software developers create, track, and assist in maintaining multiple files at once."
//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

//CODE HERE
let gitHubDefinition = "GitHub is a website that allows for the uploading, remote-storage, and shareing of software using Git.  Enabling multiple developers to easily share files and collaborate on projects."
//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

//CODE HERE
let gitInitDefinition = "git init is the command that applies the root git file to the folder that the developer wants to include for the use of git, thus enableing all other git commands."
//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

//CODE HERE
let gitCloneDefinition = "git clone creates a copy of the git tracking, creating a branch in the git system that helps to prevent the information from one git repository from getting lost or destroyed by creating a back-up in another location."
//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

//CODE HERE
let gitStatusDefinition = "git status is the command used to check on the version status of your git enabled folders.  Telling the developer what files have been changed or commited, and how they may or may not match up with the GitHub repository."
//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

//CODE HERE
let gitAddDefinition = "git add is used to specify what files or folders are to be prepared for commiting to git version control.  either by finishing the statment with a period.  Or by specifying the name of the file or folder."
let gitAddCode "git add ."
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

//CODE HERE
let gitCommitDefinition = "git commit is used to take files that have already been prepared by 'git add' and add them into the final version history for git.  allowing for tracking, retrieval, and storage."
let gitCommitCode = 'git commit -m "initial commit"'
//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

//CODE HERE
let gitPushDefinition = "git push is the command that works with the site GitHub in order to update the developer's online repositories with all the git commit's that the developer has produced."